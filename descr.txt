import subprocess
import os

os.chdir(r"C:\Program Files\IBM\clidriver\bin")

command = (
    'db2cli execsql -connstring "DATABASE=DB2B;HOSTNAME=10.10.10.10;PORT=555;'
    'PROTOCOL=TCPIP;UID=user1;PWD=pass11" -inputsql test.sql'
)

result = subprocess.run(command, shell=True, capture_output=True, text=True)

print("STDOUT:")
print(result.stdout)

if result.stderr:
    print("STDERR:")
    print(result.stderr)