На сервере выполни:
dspmq
затем:
runmqsc ИМЯ_ТВОЕГО_QM
и внутри:
DISPLAY QMGR
DISPLAY LISTENER(*)
DISPLAY CHANNEL(*)
DISPLAY AUTHINFO(*)
И пришли мне вывод этих команд (пароли, если где-то отображаются, можешь убрать).
По нему я смогу сказать тебе конкретно:
какой SVRCONN создать;
нужен ли вообще новый listener;
какой CLNTCONN создать;
какой CONNAME и QMNAME указать;
как получить AMQCLCHL.TAB;