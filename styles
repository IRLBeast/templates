def test_get_pass_exp():
    headers = {
               "username": "dGVzdHVzZXI=", #testuser
               "password": "dGVzdHVzZXIxMjNR" #testuser123Q
               }

    response = requests.get(
        f"{base_url}/login",
        headers=headers,
    )

    print(response.status_code)
    print('\n', response.text)