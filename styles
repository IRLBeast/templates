def set_dx_checkbox(page, label: str, checked=True):
    checkbox = page.get_by_role("checkbox", name=label)

    current = checkbox.get_attribute("aria-checked") == "true"
    if current != checked:
        checkbox.click()


set_dx_checkbox(page, "Полное наименование:")
page.get_by_role("textbox", name="Полное наименование:").fill(new_name)

set_dx_checkbox(page, "Основная информация о компании")
page.get_by_role("textbox", name="Основная информация о компании").fill(company_name)