# D:/Autotests/testapp/tests/aiskr/aiskr_test.py
from django.test import LiveServerTestCase
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.chrome.service import Service
import os


class CreditRegisterTests(LiveServerTestCase):
    """Тесты для кредитного регистра"""
    
    @classmethod
    def setUpClass(cls):
        super().setUpClass()
        
        # Путь к chromedriver.exe (в корне проекта)
        driver_path = os.path.join(os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "chromedriver.exe")
        
        service = Service(executable_path=driver_path)
        options = Options()
        options.add_argument("--start-maximized")
        options.add_argument("--ignore-certificate-errors")
        options.add_argument("--ignore-ssl-errors=yes")
        
        cls.driver = webdriver.Chrome(service=service, options=options)
        cls.driver.implicitly_wait(10)
    
    @classmethod
    def tearDownClass(cls):
        cls.driver.quit()
        super().tearDownClass()
    
    def test_credit_register_page(self):
        """Тест открытия страницы"""
        self.driver.get(f"{self.live_server_url}/")
        assert "Кредитный регистр" in self.driver.title
        print("✅ Страница кредитного регистра открыта")


class CreditRegisterAuthTests(LiveServerTestCase):
    """Тесты авторизации"""
    
    @classmethod
    def setUpClass(cls):
        super().setUpClass()
        
        driver_path = os.path.join(os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "chromedriver.exe")
        
        service = Service(executable_path=driver_path)
        options = Options()
        options.add_argument("--start-maximized")
        options.add_argument("--ignore-certificate-errors")
        
        cls.driver = webdriver.Chrome(service=service, options=options)
        cls.driver.implicitly_wait(10)
    
    @classmethod
    def tearDownClass(cls):
        cls.driver.quit()
        super().tearDownClass()
    
    def test_auth_page(self):
        """Тест страницы авторизации"""
        self.driver.get(f"{self.live_server_url}/AuthenticateLegal")
        assert "Вход через USBToken" in self.driver.page_source
        print("✅ Страница авторизации открыта")