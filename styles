def open_section(page, title):
    section = page.locator(
        f'.dx-accordion-item:has(.dx-accordion-item-title-caption:has-text("{title}"))'
    )

    classes = section.get_attribute("class") or ""

    if "dx-accordion-item-opened" not in classes:
        section.locator(".dx-accordion-item-title").click()