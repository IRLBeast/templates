import os
import sys

# Принудительно указываем путь к clidriver
clidriver_path = r'D:\autotest\.venv\Lib\site-packages\clidriver\bin'
os.environ['PATH'] = clidriver_path + os.pathsep + os.environ['PATH']
os.environ['IBM_DB_HOME'] = r'D:\autotest\.venv\Lib\site-packages\clidriver'

# Проверяем, что видит драйвер
print("IBM_DB_HOME:", os.environ.get('IBM_DB_HOME'))
print("PATH includes clidriver:", clidriver_path in os.environ['PATH'])