.then(data => {

    this.messageText = data.message;

    console.log("messageText =", this.messageText);

    this.messageDialog = true;

    this.$nextTick(() => {
        console.log("DOM:", document.querySelector("pre").innerText);
    });

});